С точки зрения логики, то геттеры-сеттеры не имеет смысла делать private.

-----

Относительно анонимных классов важно четко уяснить и запомнить следующее правило:

new Solution (какие-то аргументы)    { какие-то методы с их телами};

... всегда означает, что создается не объект класса Solution,
а объект НОВОГО класса, который является ПОТОМКОМ Solution (или реализует этот интерфейс).

Стоит еще обратить внимание, что хоть конструкторы в явном виде у анонимных классов объявлять нельзя,
однако неявно можно вызвать super(аргументы);

Именно таким образом, как мы сделали в задаче. В нашем случае аргументом является литерал "The Darkside Hacker"

-----

Самый популярный комментарий в дискуссии:

Что происходит в задаче:
1) Сначала вызываем метод sout() у экземпляра (класса Solution) с полем "main".

2) Метод sout создает экземпляр наследника класса Solution с полем "sout" и тут же вызывает у
этого экземпляра метод printName(). В результате у нас есть два объекта, которые связаны друг с
другом одновременно двумя способами: "внешний-внутренний" и "родитель-наследник".

3) Метод printName() вызывает метод getName(). Теперь главный вопрос - у какого экземпляра
вызывается метод getName() - у экземлпяра с "main" или с "sout"?

4) Ответ вытекает из следующих фактов: приватный метод не передается в наследуемый класс, однако
у экземпляра внутреннего класса есть доступ к приватному методу внешнего объекта (т.е. объекта с "sout").
Публичный же метод передается в наследуемый класс (и соответственно это уже свой "собственный"
метод внутреннего объекта, который ко внешнему объекту не имеет отношения).

В случае решения с помощью "super.getName()" (которое работает, но не принимается валидатором)
ситуация еще запутаннее. Надо понимать, что super.getName() - это обращение к собственному полю
name (где записано "sout"), а не к полю name внешнего объекта (где записано "main") - хоть и
обращение с помощью метода родительского класса.

-----

Из окружающего класса нет доступа к приватным методам анонимного класса 🙄
Если вызвать super.getName() [private!], то будет вызван приватный метод окружающего класса, но на экземпляре анонимного класса.

-----

Для тех кто не разобрался с наследованием в анонимном классе (почему нельзя написать this.name):
new Solution("The Darkside Hacker") {
    void printName() {
        System.out.println(getName());
    }
}.printName();

Анонимный класс это всегда наследник/имплементатор указанного в начале класса / интерфейса.
Из примера выше, анонимный класс является просто наследником Solution для которого мы добавили
метод printName(). Те грубо говоря:

public class AnonSolution extends Solution {

    // default constructor
    AnonSolution(String name) {
        super(name);
    }

    // extension to Parent
    void printName() {
        System.out.println(getName());
    }
}

Если добавить к этому то, что private элементы класса доступны только самому классу, те наследники
не имеют к ним доступа, то все становится понятно.

---

